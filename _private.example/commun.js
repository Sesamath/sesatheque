// prendre le même contenu que config.js et l'adapter pour une 2e Sésathèque
// sur le port 3002 dans la doc

// cette 2e sésathèque pourrait être dans un fichier nommé autrement mais les commandes suivantes utilisent ce nom
// `npm run build:commun`
// `npm run start:commun`
// `npm run start:both`
