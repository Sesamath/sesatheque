{#value}
  <label>{label}
    <input {?id}id="{id}"{/id} type="checkbox" name="{name}" value="{value}"{?selected} checked="checked"{/selected}{?readonly} readonly="readonly"{/readonly} />
    {?labelInfo}<span class="remarque">({labelInfo})</span>{/labelInfo}
  </label>
{/value}
