<div id="actions">
  <ul>
  {#links}
    {?spacer}
      <li class="tab-spacer"></li>
    {:else}
      <li {?id}id="{id}"{/id} {?selected}class="selected"{/selected} {?hidden}style="display: none;"{/hidden}>
        {?html}
          {html|s}
        {:else}
          {>link /}
        {/html}
      </li>
    {/spacer}
  {/links}
  </ul>
  <div class="clearfix"></div>
</div>
