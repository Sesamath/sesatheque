<div class="block ressource">
{?error}
    <p class="has-error">{error}</p>
{:else}
    <span class="publie">{?publie.value} Publié{:else} NON PUBLIÉ{/publie.value}</span>
    <span class="restriction">{?restriction.value}
        {@eq key=restriction.value value=0}Public
        {:else}
            {restriction.title} :
            {@eq key=restriction.value value=1} prof{/eq}
            {@eq key=restriction.value value=2} auteur
                {:else} {restriction.value} inconnue
            {/eq}
        {/eq}
        {:else}Public{/restriction.value}</span>
    <h2>{titre.value}</h2>
    <p>
    oid : {oid} ({origine.value}/{idOrigine.value},
    {typeTechnique.title} {typeTechnique.value},{~s}
    {version.title} {version.value})<br />
    {dateCreation.title} : {dateCreation.value}<br />
    {dateMiseAJour.title} : {dateMiseAJour.value}<br />
    {niveaux.title} : {niveaux.value}<br />
    {categories.title} : {categories.value}<br />
    {typePedagogiques.title} : {typePedagogiques.value}<br />
    {typeDocumentaires.title} : {typeDocumentaires.value}<br />
    </p>
    <p>{resume.title} : {resume.value|s}</p>
    <p>{description.title} : {description.value|s}</p>
    <p>{commentaires.title} : {commentaires.value|s}</p>
    <p>{parametres.title} : <pre>{parametres.value|s}</pre></p>

    {?auteurs.value}<ul>{auteurs.title} : {#auteurs.value}
        <li>{?url}<a href="{url}">{nom}</a> {:else}{nom}{/url}</li>
    {/auteurs.value}</ul>{/auteurs.value}

    {?contributeurs.value}<ul>{contributeurs.titre} : {#contributeurs.value}
        <li>{?url}<a href="{url}">{nom}</a> {:else}{nom}{/url}</li>
    {/contributeurs.value}</ul>{/contributeurs.value}

    {?relations.value}<ul>{relations.title}
        {#relations.value}<li>{predicat} {lien|s} ({oid})</li>{/relations.value}
    </ul>{/relations.value}<br />

{/error}
</div>
{!
<pre>
{@contextDump key="full" to="output"/}
</pre>
!}
