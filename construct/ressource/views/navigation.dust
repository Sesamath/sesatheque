{#links}
  <a href="{href}" class="mdl-button{?selected} is-active{/selected}" {?icon}title="{value}"{/icon} {attrs}>
    {?icon}
      <i class="material-icons md-48">{icon}</i> <span class="mdl-layout--large-screen-only">{value}</span>
    {:else}
      {value}
    {/icon}
  </a>
{/links}
