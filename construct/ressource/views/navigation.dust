{#links}
  <a href="{href}" {?selected}class="selected"{/selected} {?icon}title="{value}"{/icon} {attrs}>
    {?icon}
      <i class="material-icons">{icon}</i> <span>{value}</span>
    {:else}
      {value}
    {/icon}
  </a>
{/links}
